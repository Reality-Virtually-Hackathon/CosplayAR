using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MixedRealityMode : MonoBehaviour {

	// Use this for initialization
	void Start () {

        //Vuforia.VideoBackgroundManager.Instance.SetVideoBackgroundEnabled(true);
        //Vuforia.MixedRealityController.Instance.SetMode(Vuforia.MixedRealityController.Mode.ROTATIONAL_VIEWER_AR);
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
